#!/bin/bash
# Start libinput-gestures with proper checks

# Kill any existing instances
pkill -f libinput-gestures || true

# Wait a moment
sleep 2

# Start libinput-gestures
/usr/bin/libinput-gestures